// Global test setup. Kept minimal: only jest-dom matchers for now.
// Task D1 will extend this file with MSW server lifecycle hooks and a
// renderWithProviders helper.
import "@testing-library/jest-dom";
